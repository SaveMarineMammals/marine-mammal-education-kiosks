# Preview thumbs (optional)

Commit only tiny JPEG/WebP previews here (typically under 200 KB).
Full-resolution masters belong in the media store, not Git.

Expected example: `hero-thumb.jpg` referenced from `manifest.yaml`.

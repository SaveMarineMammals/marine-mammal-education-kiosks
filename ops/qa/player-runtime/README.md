# Runtime config for the headless player bind mount (`cmsSettings.xml`, etc.).

Generated by `run_qa_pipeline.py` — contents are gitignored.

Ownership: the player entrypoint chowns this mount to `QA_PLAYER_UID` /
`QA_PLAYER_GID` (host orchestrator identity on Linux) and drops root before
writing config, so CI can rewrite files without permission errors.

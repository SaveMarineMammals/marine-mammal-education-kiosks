# Ops scripts

Thin wrappers around `tools/` for common operator tasks. Prefer implementing logic once under `tools/` and calling it from here.

Examples to add later:

- `sync-exhibit.sh <slug>`
- `validate-all.sh`
- `bootstrap-check.sh` (SSH smoke test against a Pi)

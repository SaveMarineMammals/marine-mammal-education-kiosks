# Local media working directory

Place exhibit masters here while authoring, e.g.:

```text
media/<exhibit-slug>/masters/
```

This tree is **gitignored**. Upload finished assets to the media store and record them in `exhibits/<slug>/media/manifest.yaml`.
